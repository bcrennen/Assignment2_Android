<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.bcit.assignment2">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/virus"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/virus"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment2">

        <activity android:name=".CasesByMonthAndYearActivity"
            android:screenOrientation="fullSensor"/>
        <activity android:name=".CasesByHealthAuthorityActivity"
            android:screenOrientation="fullSensor"/>
        <activity android:name=".GroupByAgeScreen"
            android:screenOrientation="fullSensor"/>
        <activity android:name=".GroupByGenderScreen"
            android:screenOrientation="fullSensor"/>
        <activity android:name=".MainActivity"
            android:screenOrientation="fullSensor"/>
        <activity android:name=".AccountScreen"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>